-- Root admin is now created by scripts/setup.sh during first-time setup.
-- This migration is kept as a no-op for compatibility with existing deployments.
-- To create a root admin manually: bash scripts/setup.sh
SELECT 1;
